#class name must match the file name
class AboutController < ApplicationController   #inherit from ApplicationController to get all the functionality from rails
  def index    #it doesn't need to do anything at all   , if it does any rendering thats good, otherwise we gonna use rails default render
    
  end
end